#In here  Im going keep the record of the Program
import logging
from logging import DEBUG

logging.basicConfig(level=DEBUG,format="%(asctime)s - %(levelname)s - %(message)s",filename='log.log' ,filemode='w')

